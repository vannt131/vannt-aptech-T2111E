#include <stdio.h>
int main()
{
	int minX = arr[0];
	for (int i=0; i<n; i++)
	{
		if (arr[i]) > 0 > minX
		{
			minX = arr[i];
	    }
	}
	
	if (minX <= 0)
	{
		printf("Mang toan so am");
    }
    else
    {
    	
    }
