#include <stdio.h>
int main(){
	int n, arr[n];
	printf("Nhap so n:");
	scanf("%d", &n);
	
	for (int i=0; i<n; i++){
		printf("Nhap pt arr[%d]", i);
		scanf("%d", &arr[i]);
		if ( arr[i]%2 == 1)
		{
			int count =0
			count++;
		}
		if (max = count)
		{
			
		}
			
	}
}
